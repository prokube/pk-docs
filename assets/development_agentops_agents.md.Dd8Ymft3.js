import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.ChTAMbUy.js";const n="/docs/assets/model-configurations.jyWg7RJ_.png",s="/docs/assets/chat-tool-activity.vpVAKZRl.png",m=JSON.parse('{"title":"Agents","description":"","frontmatter":{},"headers":[],"relativePath":"development/agentops/agents.md","filePath":"versions/development/agentops/agents.md","lastUpdated":1786984507000}'),l={name:"development/agentops/agents.md"};function i(d,e,h,g,c,p){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="agents" tabindex="-1">Agents <a class="header-anchor" href="#agents" aria-label="Permalink to &quot;Agents&quot;">​</a></h1><p>prokube runs agents on <a href="https://kagent.dev/" target="_blank" rel="noreferrer">kagent</a>, a Kubernetes-native agent runtime. Agents, their model configuration, and their tools are Kubernetes custom resources, managed through the pkui <strong>Agents</strong> page in your workspace.</p><div class="info custom-block"><p class="custom-block-title">kagent documentation</p><p>Upstream references:</p><ul><li><a href="https://kagent.dev/docs" target="_blank" rel="noreferrer">kagent documentation</a></li><li><a href="https://modelcontextprotocol.io/" target="_blank" rel="noreferrer">Model Context Protocol (MCP)</a></li></ul></div><p>This page covers the prokube-specific workflow for wiring a model, tools, and an agent together, and testing the result. For tool hosting itself, see <a href="./mcp_servers.html">MCP Servers</a>.</p><h2 id="when-to-use-agents" tabindex="-1">When to Use Agents <a class="header-anchor" href="#when-to-use-agents" aria-label="Permalink to &quot;When to Use Agents&quot;">​</a></h2><p>Use the Agents page when you need a conversational or task-driven agent that calls an LLM and tools exposed through MCP servers or other agents. Use it when you want the agent to run on the platform: its configuration versioned as Kubernetes resources, observable through pkui, and reachable by other workloads over A2A.</p><h2 id="typical-workflow" tabindex="-1">Typical Workflow <a class="header-anchor" href="#typical-workflow" aria-label="Permalink to &quot;Typical Workflow&quot;">​</a></h2><p>Building a working agent is a short chain of dependent resources:</p><ol><li>If needed, store an external provider API key as a Kubernetes Secret.</li><li>Choose an available <strong>Model Configuration</strong>, or create one for an external provider or in-cluster model. Some models might already be available, if pre-configured by an administrator.</li><li>Create an <strong>Agent</strong>, attach the Model Configuration, and select MCP tools.</li><li>Test the agent in the built-in chat.</li></ol><h3 id="_1-store-the-provider-api-key" tabindex="-1">1. Store the Provider API Key <a class="header-anchor" href="#_1-store-the-provider-api-key" aria-label="Permalink to &quot;1. Store the Provider API Key&quot;">​</a></h3><p>Skip this step if you will use an admin-provided Model Configuration or an <strong>Internal</strong> (in-cluster) model. Admin-provided configurations use a credential the administrator already manages centrally. Internal models don&#39;t need a stored credential at all: the platform authenticates them automatically as in-cluster workloads.</p><p>Otherwise, for an <strong>external</strong> provider (OpenAI, Anthropic, Gemini), create a Kubernetes Secret first: open the user menu (top right) → <strong>K8s Secrets</strong> → <strong>Add Secret</strong>, and store the provider API key as a key/value pair in the workspace, for example <code>ANTHROPIC_API_KEY: sk-ant-...</code> or <code>OPENAI_API_KEY: sk-...</code>. See <a href="./../platform/kubernetes.html#kubernetes-secrets">Kubernetes Secrets</a> for the full flow.</p><h3 id="_2-choose-or-create-a-model-configuration" tabindex="-1">2. Choose or Create a Model Configuration <a class="header-anchor" href="#_2-choose-or-create-a-model-configuration" aria-label="Permalink to &quot;2. Choose or Create a Model Configuration&quot;">​</a></h3><p>Check <strong>Model Configurations</strong> on the <strong>Agents</strong> page first. An administrator may already have granted the workspace access to an external model through AI Gateway. These configurations appear automatically with <strong>AI Gateway</strong> as their origin and can be selected when creating an agent.</p><p><img src="'+n+'" alt="Model Configurations list with an AI Gateway model" loading="lazy"></p><p>If no suitable configuration exists, select <strong>Create ModelConfig</strong>. The <strong>Provider</strong> field selects how kagent reaches the backend:</p><table tabindex="0"><thead><tr><th>Provider</th><th>Type</th><th>Needs a Secret?</th></tr></thead><tbody><tr><td>OpenAI (external)</td><td>External</td><td>Yes</td></tr><tr><td>Anthropic (external)</td><td>External</td><td>Yes</td></tr><tr><td>Gemini (external)</td><td>External</td><td>Yes</td></tr><tr><td>Internal - OpenAI-compatible</td><td>In-cluster</td><td>No</td></tr><tr><td>Internal - Ollama</td><td>In-cluster</td><td>No</td></tr></tbody></table><ul><li>For an <strong>external</strong> provider, select the Secret and the secret key created in step 1, and enter the model name.</li><li>For <strong>Internal - OpenAI-compatible</strong>, either pick a <strong>Ready</strong> model deployed through <a href="./llm_serving.html">LLM Serving</a> in the workspace, or enter a custom in-cluster OpenAI-compatible base URL and model name manually.</li><li>For <strong>Internal - Ollama</strong>, enter the in-cluster Ollama host, for example <code>http://ollama.&lt;workspace&gt;.svc.cluster.local:11434</code>.</li></ul><p>The structured form covers the common presets above. For another kagent-supported provider, open the Model Configuration YAML editor and define its provider-specific fields. OpenAI-compatible services can use the <code>OpenAI</code> provider with an <code>openAI.baseUrl</code> and a workspace Secret containing the API key; Azure OpenAI uses its dedicated kagent provider configuration. This keeps the endpoint and credential within the workspace configuration.</p><p>When provider credentials and model access are managed centrally, administrators can configure Azure OpenAI, Mistral AI, GitHub Models, or another OpenAI-compatible endpoint through Agent Gateway. Granted models then appear in the workspace as <strong>AI Gateway</strong> Model Configurations. See <a href="./../admin/external_models.html">External Models</a> for the administrator workflow.</p><div class="info custom-block"><p class="custom-block-title">Tool calling with internal models</p><p>For a self-hosted text-generation model used with MCP tools, enable <strong>automatic tool calling</strong> in <a href="./llm_serving.html#enable-tool-calling-for-agents">LLM Serving: Enable Tool Calling for Agents</a> and select the parser that matches the model. This option is available for the vLLM and HuggingFace runtimes.</p></div><h3 id="_3-create-an-agent" tabindex="-1">3. Create an Agent <a class="header-anchor" href="#_3-create-an-agent" aria-label="Permalink to &quot;3. Create an Agent&quot;">​</a></h3><p>Still on the Agents page, open <strong>Configured Agents</strong> → <strong>Create Agent</strong>. Choose an <strong>Agent type</strong>:</p><ul><li><strong>Declarative</strong>: the common case. Requires a <strong>Name</strong>, the <strong>Model Configuration</strong> from step 2, and a required <strong>System message</strong> describing the agent&#39;s behavior.</li><li><strong>BYO</strong>: wraps your own container image, invoked over A2A, instead of a declarative kagent agent.</li></ul><p>For a declarative agent, attach tools:</p><ul><li><strong>MCP tools</strong>: a searchable, filterable table of every tool discovered from MCP servers (Tools) available to the workspace. Check individual tools, or use <strong>&quot;Use all current and future tools from <code>&lt;server&gt;</code>&quot;</strong> per server to keep the agent in sync with a tool server automatically instead of a fixed snapshot.</li><li><strong>Agent tools</strong>: let this agent call other agents in the same workspace as tools.</li></ul><p>MCP tools come from <strong>Tools</strong> on the same Agents page. Connect an MCP endpoint there first if the tool you need isn&#39;t listed yet. See <a href="./mcp_servers.html">MCP Servers</a> for details. If the agent uses <a href="./sandboxes.html">Agent Sandboxes</a>-backed skills, attach the Tool that points at <code>sandbox-mcp</code>.</p><h3 id="_4-test-the-agent" tabindex="-1">4. Test the Agent <a class="header-anchor" href="#_4-test-the-agent" aria-label="Permalink to &quot;4. Test the Agent&quot;">​</a></h3><p>Open the agent from the Agents list. The detail page&#39;s <strong>Chat</strong> tab is available once the agent is <code>Ready</code>, and shows:</p><ul><li>A session sidebar (new/previous sessions).</li><li>The chat itself.</li><li>A <strong>Connected Tools</strong> panel listing attached tools and their readiness.</li></ul><p>Each response that used a tool shows a <strong>Tool Activity</strong> entry with the tool&#39;s arguments and result, so you can verify what the agent actually called and with what inputs. This is useful for debugging both prompt and tool wiring before relying on the agent elsewhere.</p><p><img src="'+s+'" alt="Agent chat showing connected tools and tool activity" loading="lazy"></p><p>For programmatic access instead of the chat UI, the agent&#39;s Overview tab lists its A2A endpoint. External callers need an API key scoped to the agent (<code>a2a</code>). See <a href="./../platform/api_keys.html">API Keys</a>. Callers inside the same workspace can reach the agent over the internal A2A path without a key.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./agent_gateway.html">Agent Gateway</a></li><li><a href="./llm_serving.html">LLM Serving</a></li><li><a href="./mcp_servers.html">MCP Servers</a></li><li><a href="./sandboxes.html">Agent Sandboxes</a></li><li><a href="./memory_stores.html">Memory Stores</a></li><li><a href="./../admin/external_models.html">External Models</a></li><li><a href="./../platform/api_keys.html">API Keys</a></li><li><a href="./../platform/kubernetes.html">Kubernetes Resources</a></li></ul>',35)])])}const f=t(l,[["render",i]]);export{m as __pageData,f as default};
